<br/>

<div class="booyah-box col-xs-10 col-xs-offset-1">
  <h1 class="text-center">Learn Something Today!</h1>
  <br/>
  <% @courses.each do |course| %>
    <h2 class="test-center"><%= link_to course.title, course_path(course) %></h2>
    <br/>
    <div class="col-xs-12 col-sm-8">
      <%= course.description %>
    </div>
    <div class="col-sm-4 col-xs-12 text-center">
      <%= link_to 'Course Details', course_path(course), class: 'btn btn-primary btn-lg' %>
    </div>
    <br class="clear"/>
  <% end %>
</div>